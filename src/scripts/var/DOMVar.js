const search = document.querySelector('[data-search]');
const searchbtn = document.querySelector('[data-searchBtn]');
const weatherDisplay = document.querySelector('[data-weather]');
const todayTemp = document.querySelector('[data-todayTemp]');
const todayFeels = document.querySelector('[data-todayFeels]');
const todayWeather = document.querySelector('[data-todayWeather]');
const city = document.querySelector('[data-city]');
const todayIcon = document.querySelector('[data-todayIcon]');
const day = document.querySelector('[data-day]');
const desc = document.querySelector('[data-desc]');
const dayTemp = document.querySelector('[data-dayTemp]');
const nightTemp = document.querySelector('[data-nightTemp]');
const humidity = document.querySelector('[data-humidity]');
const pressure = document.querySelector('[data-pressure]');
const visibility = document.querySelector('[data-visibility]');
const tempMin = document.querySelector('[data-tempMin]');
const tempMax = document.querySelector('[data-tempMax]');

export {
  search,
  searchbtn,
  weatherDisplay,
  todayTemp,
  todayFeels,
  todayWeather,
  city,
  todayIcon,
  day,
  desc,
  dayTemp,
  nightTemp,
  humidity,
  pressure,
  visibility,
  tempMin,
  tempMax
};
