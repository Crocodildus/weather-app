const currentTemp = document.querySelector('[data-currentTemp]');
const currentFeels = document.querySelector('[data-currentFeels]');
const currentWeather = document.querySelector('[data-currentWeather]');
const currentIcon = document.querySelector('[data-currentIcon]');
const humidity = document.querySelector('[data-humidity]');
const pressure = document.querySelector('[data-pressure]');
const visibility = document.querySelector('[data-visibility]');
const tempMin = document.querySelector('[data-tempMin]');
const tempMax = document.querySelector('[data-tempMax]');

export {
  currentTemp,
  currentFeels,
  currentWeather,
  currentIcon,
  humidity,
  pressure,
  visibility,
  tempMin,
  tempMax
};
